services:
  - type: web
    name: claude-explorer
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/web/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
